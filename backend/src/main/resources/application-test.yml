spring:
  config:
    import: optional:file:env/.env.test.properties

management:
  health:
    redis:
      enabled: false
    elasticsearch:
      enabled: false
  endpoint:
    health:
      show-details: never

rate-limit:
  auth-per-minute: 10000
  api-per-minute: 100000
